services:
  - type: web
    name: startupx-system
    env: python
    buildCommand: pip install -r requirements_final.txt
    startCommand: python launch_zyndai.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
      - key: PORT
        value: 10000
